# Copy this file to `.env` and fill in real values for local development.
# `.env` itself is gitignored — never commit credentials.
# Read at startup by at.ac.fhcampuswien.EnvLoader and exposed as System properties
# (DatabaseUtil reads them via System.getProperty with defaults "user" / "pw").

DB_USER=user
DB_PASSWORD=pw
